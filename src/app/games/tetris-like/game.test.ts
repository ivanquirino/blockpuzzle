import { rotateClockwise, pieceI, pieceL } from "./game";

describe("rotation/rotateClockwise", () => {
  test("findPivot", () => {
    const rotated = rotateClockwise(pieceI, 2);

    

    // expect(rotated).toEqual({ x: 5, y: 1 });
  });
});
